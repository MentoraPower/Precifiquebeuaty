import { ListScreenSkeleton } from '@/components/skeletons'

export default function Loading() {
  return <ListScreenSkeleton title="Investimentos" subtitle="Equipamentos e itens duráveis." withButton rows={3} />
}
